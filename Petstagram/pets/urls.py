from django.urls import path, include

from Petstagram.accounts import views

urlpatterns = (
    # path('pets/add/', views.pet_add_page, name='home page'),
    # path('pets/<str:username>/pet/<slug:pet_slug>/', views.pet_details_page, name='pet details page'),
    # path('pets/<str:username>/pet/<slug:pet_slug>/edit', views.pet_edit_page, name='pet edit page'),
    # path('pets/<str:username>/pet/<slug:pet_slug>/delete', views.pet_delete_page, name='pet delete page'),
)


